name: clean_arch_flutter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  rxdart: ^0.24.1
  bloc: ^6.1.0
  flutter_modular: ^2.0.1
  dio: ^3.0.10
  flutter:
    sdk: flutter

  dartz:

  cupertino_icons: ^1.0.0

dev_dependencies:
  mockito: ^4.1.3
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
